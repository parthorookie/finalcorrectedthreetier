apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: worker
  labels:
    app: worker
spec:
  type: ClusterIP
  selector:
    app: worker
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP